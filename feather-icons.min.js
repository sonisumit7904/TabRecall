// Feather Icons v4.29.0 - MIT License
// This is a simplified version of feather-icons for demonstration
(function(global, factory) {
  typeof exports === "object" && typeof module !== "undefined" ? module.exports = factory() : typeof define === "function" && define.amd ? define(factory) : (global = typeof globalThis !== "undefined" ? globalThis : global || self, global.feather = factory());
})(this, function() {
  "use strict";
  
  // Icon definitions would be here in the full library
  const icons = {};
  
  // Core functionality
  const feather = {
    icons: icons,
    replace: function(attrs) {
      if (typeof document === "undefined") {
        throw new Error("The `feather.replace()` method only works in a browser environment.");
      }
      const elements = document.querySelectorAll("[data-feather]");
      Array.from(elements).forEach(element => {
        return this.replaceElement(element, attrs);
      });
    },
    replaceElement: function(element, attrs) {
      const elementAttrs = element.attributes;
      const name = elementAttrs["data-feather"].value;
      const svgString = this.icons[name] || `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="16"></line><line x1="8" y1="12" x2="16" y2="12"></line></svg>`;
      
      // Create SVG element
      const svgElement = document.createElementNS("http://www.w3.org/2000/svg", "svg");
      svgElement.innerHTML = svgString;
      
      // Replace original element with SVG
      element.parentNode.replaceChild(svgElement, element);
    }
  };
  
  return feather;
});
